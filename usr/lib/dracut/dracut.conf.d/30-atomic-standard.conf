# Include LVM and LUKS/crypt support. Anaconda commonly sets up LUKS+LVM
# and these modules must be present when regenerating the initramfs.
# See: https://gitlab.com/fedora/bootc/base-images/-/blob/main/standard/initramfs-full.yaml
add_dracutmodules+=" lvm crypt "
